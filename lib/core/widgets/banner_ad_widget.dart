import 'package:flutter/material.dart';

/// 広告ウィジェット（ローカル専用版では未使用）
class BannerAdWidget extends StatelessWidget {
  const BannerAdWidget({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
